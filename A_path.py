# (PART A) IMPORTS
import os

# (PART B) COMMON PROJECT PATH
base = os.path.dirname(os.path.realpath(__file__))
output = os.path.join(base, "output")
proc = os.path.join(base, "processes")
static = os.path.join(base, "static")